# 给 Codex 的指令 · 线上系统盘点 v2（复制粘贴即用）

> 背景：我要请工程师（Kimi + GPT 交叉验证）评估"线上后端是重做、改造、还是保留"。
> **本阶段只做审计：不修改任何代码、不动数据库、不重启服务、不部署。**
> 技术栈按实际来：Python 服务 + SQLite + Nginx（不是云函数项目，下面的措辞以此为准）。

---

## 第 0 件：先确定 Source of Truth（最重要，先做这件）

工作电脑本地代码、GitHub、服务器实际运行的代码**可能不是同一个版本**（之前发生过）。所以第一步：

1. 正式环境 `/tomorrow` 当前由服务器上**哪个目录**提供？
2. 当前运行的 service / PID / 启动命令是什么（systemd？supervisor？nohup？）
3. 该运行目录的 Git commit 是什么？**`git status` 是否干净？如存在未提交修改，列出修改文件清单和 diff 摘要**（防止"commit A + 服务器上私改"的情况被误判为 commit A）。对应 GitHub 哪个 commit？
4. 工作电脑本地代码和服务器运行代码有没有 diff？有就列出来
5. Nginx 配置里 `/tomorrow` 及相关路由指向哪里（贴配置段落）

**结论写清楚：本次审计的对象 = 服务器上实际运行的那份代码。** 后续所有盘点都针对它，不是针对工作电脑本地副本。

## 第一件：代码与结构盘点

对第 0 件确认的生产代码：

1. 目录树（2 层）+ 每个顶层目录/主要文件的职责一句话
2. 当前 commit hash + 最近 10 条与菜单/推送/库存相关的 commit 信息（不要全量历史）
3. `app.py`（或主入口）的结构：哪些路由、各路由调用链
4. 写死的假数据 / 占位逻辑 / TODO 清单

## 第二件：调用关系图（替代"云函数清单"）

画出 **API endpoint → service → 规则 → 数据库** 的调用链，例如：

```text
GET /tomorrow → menu_service → recommend / replace / availability → SQLite
```

每个主要端点一条链。重点标出这几个逻辑分别在哪个文件哪个函数：

- 菜单生成（推荐逻辑）
- 换菜 / 替换逻辑
- 库存判断（食材充足度怎么算、在哪算、改库存后谁触发重算）
- 推送逻辑（给谁推、什么时候推、按什么规则选人）
- 双厨房（深圳/香港）切换逻辑：数据是隔离的还是同一份

## 第三件：数据库盘点（不要整库搬运）

1. SQLite schema 全量导出（`sqlite3 .schema` 即可）
2. 关键表之间关系一句话说明（dishes / pantry / meal_plans / 其他）
3. 每张表 2~3 条**脱敏**示例数据（无个人信息）
4. 各表行数统计（菜品多少条、食材多少条、meal_plans 多少条）

## 第四件：差异对照 + 改造评估

对照附件 `NEW_VERSION_BACKEND_NEEDS.md`（我会一并提供），逐节回答：

1. 新版需要的字段/结构，现有 schema 有哪些、缺哪些
2. 缺的每项：**加字段/加表/拆表能否解决？** 波及哪些代码？（注意：结构不同 ≠ 必须重写，我要的是迁移成本评估）
3. 菜单生成/推荐/换菜/库存/推送五块逻辑，每块自评：可直接复用 / 建议重构 / 应该重写，给理由
4. `app.py` 混杂结构的耦合点在哪？继续在上面堆新功能（每餐独立确认、饮品位、每日备注）会不会越补越乱？

## 第五件：测试现状

检查项目现有测试工具：**如已有与正式系统匹配的 smoke/regression 测试，运行并记录结果**；没有就写"无测试"。**不要为了本次审计新写测试框架。**

---

## 交付物

一个文件夹（或 zip）`prod_audit/`，含：

- `SOURCE_OF_TRUTH.md`（第 0 件结论）
- `CODE_STRUCTURE.md` + 调用关系图（第一、二件）
- `schema.sql` + `sample_data.md` + 行数统计（第三件）
- `DIFF_VS_NEW_UI.md`（第四件，逐条对照）
- `TEST_RESULTS.md`（第五件）
- 生产代码快照 zip（排除 venv/__pycache__/日志；**不含生产数据库文件本身**；**排除 `.env`、SSH key、API Token、数据库密码、Cookie、证书私钥及一切 credentials——这些配置只记录"存在及用途"，不导出具体值**）

全程只读操作，零修改、零部署。
