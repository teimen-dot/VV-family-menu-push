# PM_HANDOFF.md — 执行交接摘要（PM 签发，2026-08-20）

> 本文档是 PM 侧工作的最终交接物。**14 项验收的执行者（AI 或人工）从这里开始。**

## 一、一句话现状

Phase 2 可操作测试版的**技术准备已 100% 完成**：环境、端点、UI、写链路、测试基线全部验证通过。
剩下的只是**按手册逐项走查 + 截图 + 填跟踪表**（14 项，当前 0/14）。

## 二、执行者必读顺序（强制）

1. `PM_GATE.md` — 门禁红线（冻结 T003、提交三问、禁止范围）
2. `ACCEPTANCE_TRACKER.md` — 验收看板（14 项状态，完成即填 ✅ + 证据）
3. `API_MAP.md` — 端点地图 + 全部 PM 实测结论（写链路 10/10 已实测）
4. `EXECUTION_GUIDE.md` — **14 项验收操作手册（照单执行，禁止自由发挥）**

## 三、执行前提（PM 已确认）

- 预览服务运行中：`http://127.0.0.1:18765`，健康检查 OK
- 登录用户：`vivian`（Owner）；**密码需向用户索取**（不在代码/文档中）
- 写库：`assets/test-family_menu.db`（24 menus / 266 items / 2095 events，`quick_check: ok`）
- 今日菜单 menu 189 = 2026-08-20 draft 4 人，可直接验收
- 测试闸门：`python3 -m unittest <file> -v`（本机无 pytest）

## 四、当前验收状态

| 通过 | 部分 | 未开始 |
|---|---|---|
| 0/14 | 2（#3 换菜、#14 深港切换——T002 曾验但被 T003E 覆盖，需重走查） | 12 |

## 五、已记录的风险/注意（勿重复踩坑）

1. `writable/family_menu.db` 是**空库**，禁止写入/误连；预览连的是 assets 测试库。
2. events 时间戳是 **UTC**（核对时间 +8 换算 HKT）。
3. readonly 是**命名残留**，实际写模式已开（bootstrap `readonly: False`）。
4. 4 天锁定池内的菜添加返回 False 是**规则正确生效**，不是 bug。
5. adjudication 测试 1 项失败 = **测试环境缺陷**（本地 backup 源空库），有修复指引，不影响 14 项验收。

## 六、完成定义（PM 判定达标的唯一标准）

- [ ] ACCEPTANCE_TRACKER.md 14 项全部 ✅，每项有证据（截图/测试/报告+行号）
- [ ] 全量测试无新增失败（基线 190/191，见 API_MAP.md）
- [ ] 正式库/golden 零修改、`PUSH_ENABLED=false` 保持
- [ ] 浏览器控制台无新增 error

## 七、收尾动作（14 项全 ✅ 后）

1. 全量测试确认无新增失败
2. 正式库 SHA 对比确认零修改
3. 本地 commit（不 push），保持预览服务运行
4. 停下交用户/Kimi 复核

---

*PM 交接完毕。执行者无需任何探索，按手册执行即可。任何超出手册范围的改动（新增端点、改规则、重构）都视为无用功。*
