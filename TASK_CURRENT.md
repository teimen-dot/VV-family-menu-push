TASK_CURRENT

> ## ⛔ PM 门禁（2026-08-20 生效，最高优先级，用户已确认）
>
> 1. **冻结 T003 系列**：禁止继续打磨 `rule_engine.py` 配餐规则；禁止新建 T003J/K 及任何
>    新的 `test_t003*.py` 与 validation DB 快照。已提交的 T003 封存不回滚不扩展。
> 2. **唯一产出标准**：本文件末尾的 **14 项浏览器真实验收** 逐项通过，每项必须有
>    "UI → 真实 API → test DB → 刷新后仍保持"的证据（截图/测试/报告节）。
> 3. **提交门禁**：每次提交前回答——对应哪一项验收？证据在哪？是否碰了 rule_engine.py？
>    三问不满足禁止提交。
> 4. **允许范围**：仅餐单/食材/菜品三页写操作、手机 UI 尺寸修复、缺图映射、历史只读读取、
>    最小新增端点（复用 menu_service/inventory/db）。禁止新造规则、mock、DOM 假写。
> 5. 详细条款见工作区 `PM_GATE.md`，与本文件冲突时以门禁为准。

**工作区：**
`~/workbuddy/claw-family-ui-phase2-writable`

开始前确认当前分支包含第2棒及登录修复 `681ee6e`；状态异常立即停止。

**唯一允许写入：**
`~/workbuddy/claw-family-ui-phase2-assets-20260819/test-family_menu.db`

禁止：

* `/tmp`、`/private/tmp`
* dirty 主工作区
* 正式数据库
* 正式部署
* 实际推送

## 目标

把当前四 Tab 真数据版升级为**完整可操作测试版**。
所有操作必须：

**UI → 真实 API → test DB → 刷新后仍保持**

不得存在 DOM 假写。

## 1. 餐单页开放真实写操作

接通现有 UI：

* 修改每餐 `diners_count`
* 备注新增/修改
* 随机换菜
* 搜索换菜
* 添加/删除菜品
* AI 智能补充
* 重新生成
* 确认此餐
* 取消/恢复餐次
* 加一餐
* 早餐饮品选择/修改

要求：

* generate / AI Fill / reconcile 只认 `diners_count`
* 不恢复成员名单人数链或餐单家宴模式
* 菜品侧家宴菜仍可搜索、手动加入
* 真实写操作保留 audit/event

## 2. 食材页开放真实写操作

接通：

* 搜索/添加食材
* 常用食材快速加入
* 快过期/撤销
* 已用完
* 最近消耗
* 补货恢复

刷新后必须与 DB 一致。

### 食材 UI

食材条目去掉图片，只保留：

* 中文名
* 英文名
* 状态/操作按钮

不维护食材图片资产。

## 3. 菜品页开放真实写操作

接通：

* 快速新增菜品
* 编辑菜品
* 收藏/取消收藏
* 家宴菜标签
* 分类、餐别、食材等现有字段
* 手动加入餐单

全部写 test DB，刷新后保持。

菜品侧 `banquet` 语义必须保留。

## 4. 手机 UI 修复

### 早餐列表尺寸统一

无论 4 道、8 道或更多：

* 图片尺寸
* 中英文文字尺寸
* 收藏/换菜/搜索/删除按钮
* 行高

必须统一。

菜多只允许页面向下增长，不得自动压缩。

### 缺图复核

重点检查：

* 榨菜肉丝
* 煎鸡翅
* 红烧肉

服务器有真实文件 → 修正映射。
服务器没有 → 保持 fallback，并列入缺图名单。
不得生成假图片。

## 5. 历史页

历史页继续只读。

餐单确认/修改后的真实历史结果必须能正常读取，不生成静态假记录。

## 补充硬性要求

### A. 全局解除只读模式

* 餐单、食材、菜品、历史四 Tab 的捕获阶段写拦截全部移除
* 所有「只读预览 / Read only / 暂未开放」标识删除
* bootstrap `readonly` 同步为 `false`
* 不得再出现「当前为只读预览」提示

历史页本身仍保持业务只读，仅删除全局 Phase 2 “预览锁”。

### B. 缺 API 时允许新增最小端点

若现有 UI 缺少真实写 API，可以新增最小后端端点，但：

* 必须复用 `menu_service / inventory / db` 已有业务函数
* 不得另造简化版业务逻辑
* 不得改变现有规则语义
* 完成回复中逐一列出新增 API

### C. 开写前建立还原点

任何写功能开发前，先复制：

`test-family_menu.db`
→
`test-family_menu.before-batch3.db`

放在同一持久资产目录。

确认快照成功后才允许开始写入测试库。

## 安全与回归

必须保持：

* 正式库/golden 零修改
* `PUSH_ENABLED=false`
* `PUSH_SCHEDULE_ENABLED=false`
* `legacy_schema_safe` 完整保留
* preview/production Cookie 分支不回归
* LAN/iPhone 登录正常
* 浏览器控制台无新增 error
* 全量测试无新增失败

## 浏览器真实验收

至少完成并刷新确认：

1. 人数 4→3
2. 新增/修改备注
3. 换菜
4. 添加菜
5. 删除菜
6. AI Fill
7. 确认一餐
8. 添加食材
9. 标记快过期
10. 用完→最近消耗→补货恢复
11. 新增/编辑菜品
12. 早餐8道菜尺寸与4道菜一致
13. 食材列表无图片
14. 四 Tab + 深港切换正常

## 完成保存流程

测试通过后：

1. 立即本地 commit
2. 不 push
3. 保持预览服务运行
4. 停下交 Kimi 复核

回复只需：

1. 修改文件
2. 餐单写功能结果
3. 食材写功能结果
4. 菜品写功能结果
5. 新增 API 清单
6. UI 修复结果
7. 缺图名单
8. audit/events 结果
9. 浏览器持久化验收
10. 全量测试
11. test DB / 正式库隔离确认
12. before-batch3 快照路径
13. 本地 commit SHA
14. 尚未完成问题
