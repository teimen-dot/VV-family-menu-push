# TASK_CURRENT：导出正式家庭菜单数据库供 Kimi 做数据迁移盘点

> v2（2026-08-10 修订）：采纳评审意见——固定导出变量防通配符冲突、213 仅作参考不作校验、必须先确认正式环境实际 DB 路径（避免旧版本信息污染）。Kimi 侧确认：需要完整 DB（含菜品↔食材关联表、分类表、库存表），仅两张表的 CSV 不够。

## 目标

从当前正式服务器确认实际 SQLite 数据库路径，并生成一个一致性只读副本，交给雇主用于 Kimi 分析菜品、食材及相关数据结构。

## 执行

1. 先确认当前正式服务**实际使用**的数据库路径，不要仅根据旧记录假设。
   预期可能是：

   ```bash
   /opt/family-menu/data/family_menu.db
   ```

   如不一致，以当前正式服务实际配置为准（可查进程参数 / systemd unit / 环境变量 `FAMILY_MENU_DB_PATH`），并报告真实路径。

2. 不停服务、不修改数据库、不修改代码。

3. 使用 SQLite backup 生成一致性副本：

   ```bash
   EXPORT=/tmp/family_menu_export_$(date +%Y%m%d_%H%M%S).db
   sqlite3 <实际数据库路径> ".backup '$EXPORT'"
   ```

   （固定使用 `$EXPORT` 变量，不要用 `/tmp/family_menu_export_*.db` 通配符——多个导出文件并存时通配会匹配多个文件导致报错。）

4. 检查导出文件可以正常打开，并报告：

   - 导出文件路径
   - 数据库全部表名
   - `dishes` 总记录数及当前 active 记录数（若字段支持）
   - `ingredients` 总记录数
   - 文件大小

   **只报告实际结果。** 雇主上次看到约 213 道菜，但那只是参考快照，正式库真实数量可能已变化；不要为凑预期数字修改或过滤数据。

5. 将生成的 `.db` 文件交给雇主（scp 或任何现成文件传输方式）。

## 限制

- 不部署
- 不改代码
- 不改正式数据库
- 不停服务
- 不动 nginx
- 不需要照片资源
- 不需要额外制作 JSON/CSV
- 不需要扫描整个项目

完成后只说明：实际数据库路径、导出文件、表数量、菜品/食材数量、检查结果。
