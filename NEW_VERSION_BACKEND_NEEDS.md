# 新版 UI/模块 → 对后端的要求清单（裁决对照表）

> 来源：家里电脑新版 `family-menu.html`（3320 行，2026-08-09）+ REQUIREMENTS_V2.md + ITER_ACCEPTANCE.md
> 用法：拿到 Codex 的 `SYSTEM_AUDIT.md` 后逐条对照。**"有且字段对齐"= ✅ 保留；"没有但好加"= ⚠️ 改造；"结构冲突"= ❌ 重做信号。**

## A. 数据模型要求

### A1. dishes 集合（新版前端直接依赖的字段）
| 字段 | 新版用法（family-menu.html 行号） | 线上是否必须具备 |
|---|---|---|
| zh / en | 全站显示 + 搜索（2265） | 必有 |
| cat | 8 分类：meat/sea/veg/soup/staple/egg/cold/onepot/fruit（3107-3111，protein 筛选 = meat+sea） | 枚举值必须一致，否则迁移断裂 |
| emoji | 按分类默认值，可改（3151） | 易加 |
| avail | ok/low/miss 三态（2318），**必须由 pantry 推导，不是人工字段** | 结构冲突高发区 ❌ |
| drink + 子类 | 饮品独立于普通菜（DRINK_DB 有 sub：豆浆类/奶类/咖啡/养生汤/茶，2644） | 线上若无独立饮品概念 = ⚠️ |
| manualOnly / 家宴 / staple_type / soup_type | 推送引擎入池规则需要（规格 §4） | 见 C 节 |
| 必需食材 / 包含蔬菜 / 照片 | 菜品编辑弹框（3233+）+ 缺料计算 | 必有 |

### A2. meal_plans 集合（每餐级，不是每天一条）
| 字段 | 新版用法 | 说明 |
|---|---|---|
| status: pending/confirmed/cancelled | 每餐独立确认（2764 setConfirmed）+ 取消软删除（3036 data-cancelled） | **整单 confirmed 的旧模型 = ❌ 结构冲突** |
| mtype | 5 种：早/午/下午茶/晚/宵夜（3009） | 旧版若固定 4 餐位 = ⚠️ |
| drinks[] | 早餐饮品位，独立于 dishes[]，不进计数（2657-2692） | 旧模型几乎肯定没有 = 需加 |
| note（每餐） | 每餐备注，保姆做菜看（2778 makeEditable） | 易加 |
| source | 预留 'auto'/'manual'/'calendar'（iCloud 联动占位） | 易加 |

### A3. day_notes 集合（每天一条）
- 当日备注，给保姆看的一天级提示（3088-3104）

### A4. pantry 集合
- 状态只有 **充足/缺货** 两态（验收 1.5 已裁决：删除适量/少量）
- 食材变更必须触发相关菜品 avail 重算（验收 4.3）
- 缺料分级：low（差少量）/ miss（缺食材），按食材聚合出采购清单（2798-2809）

## B. 业务规则要求（新版前端已写死的交互，后端必须配合）

1. **默认三餐**：每天只默认渲染早/午/晚，下午茶/宵夜经"加一餐"出现（3080-3086）→ 推送引擎不得自动推送下午茶/宵夜
2. **加一餐规则**：今天可加全部 5 种；其他天只可加下午茶/宵夜 + 加回已取消餐（3034-3041）
3. **每餐独立确认**：确认进度按餐计数（2788 updateConfirmProgress），确认后修改自动回到待确认
4. **双厨房隔离**：深圳/香港两份独立 pantry + meal_plans（REVIEW B3：只换皮是 Bug）
5. **饮品不进任何统计**：不计道数、不进模板校验、不进自动推送

## C. 推送引擎要求（Iter 2 核心，重做与否的最大权重项）

- 输入：日期 + 人数 + 库存；输出：4 天 × 各餐 dishId + 每道菜 missingIngredients
- 不重复约束：**按天去重**（前 3 天 + 今天锁定），禁止按"最近 N 道菜"去重
- 只从 avail=ok 池出菜；manualOnly / drink 不进池
- 池不足时降级：允许重复 + 返回补录提示，不报错
- 早餐结构：粥1+包点1+蛋1+豆腐1+粗粮≥1+蛋白质1–2+蔬菜按人数；午餐 quick_soup；晚餐 slow_soup
- 智能补充 = 单餐模式按缺失分类补；onepot 在场不乱补
- **裁决要点：规则集中在一个函数/配置里 = 可改；散落各处写死 = 重做**

## D. 基础设施要求

| 项 | 要求 | 对照点 |
|---|---|---|
| 中英翻译 | 云函数接 LLM + ZH_EN_DICT 词典兜底（2328-2373） | 线上若纯前端词典 = ⚠️ 需挪后端 |
| 拍照识别食材 | 云函数调 Qwen-VL，key 只存环境变量（Iter 4） | 没有 = 新增，不算重做理由 |
| 鉴权 | 匿名登录 + 家庭口令 + 角色字段（雇主/家人/保姆） | 角色分不清 = ❌ |
| 数据迁移 | 旧库菜品/食材全量迁移 + 条数对账 | 已迁移且对账通过 = 保留理由 |

## E. 裁决打分卡（拿到 SYSTEM_AUDIT.md 后填）

| 维度 | 权重 | 保留信号 | 重做信号 |
|---|---|---|---|
| meal_plans 每餐级 status | ★★★ | 已是每餐一条 | 整单一个 confirmed 字段 |
| avail 推导 | ★★★ | 由 pantry 自动推导 | 人工维护字段 |
| 推送引擎结构 | ★★★ | 规则集中、按天去重 | 按道菜数去重/规则散落 |
| 菜品分类枚举 | ★★ | 8 分类对齐 | 分类体系不同且无映射 |
| 双厨房 | ★★ | 数据隔离 | 混表只换 banner |
| 前端代码 | ★ | — | 无所谓，前端反正用新版重写 |

**预判（待验证）**：前端必然用新版重写（线上前端代码保留价值 ≈ 0）；裁决实际只在后端——数据层大概率可保留，推送引擎是重做与否的真正战场。
