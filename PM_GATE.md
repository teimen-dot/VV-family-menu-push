# PM_GATE.md — 产品经理门禁（2026-08-20 生效，强制阅读）

> 本文件由产品经理（PM）在用户确认偏差后发布。任何 AI（Kimi / Codex / Claude / 其他）
> 在工作区 `claw-family-ui-phase2-writable` 内继续工作前，必须先读本文件。
> **违反本门禁的改动一律视为无用功，会被回滚并追责。**
>
> **执行入口：先读 `PM_HANDOFF.md`（一句话现状 + 必读顺序 + 完成定义），再按 `EXECUTION_GUIDE.md` 逐项执行。**

---

## 一、冻结令：T003 系列立即停止（红线 #1）

- ❌ 禁止继续打磨 `rule_engine.py` 的配餐规则（早餐八槽、蛋白质槽、section 14/15、
  轮换降级、豆腐证据、任何"新造复杂规则"）。
- ❌ 禁止新建 T003J / T003K / 任何新的 `test_t003*.py`、新的 validation DB 快照。
- ❌ 禁止以"规则审计"名义再做整轮生成→校验→再生成循环。
- ✅ 已提交的 T003 代码不回滚、不删除，但**封存**，不再扩展。

**理由（用户确认）**：T003 系列 9 轮迭代产出 12+ 份测试 DB 快照和 84KB 规则引擎，
属于"花了非常多时间调测试版本"的无用功，偏离了 TASK_CURRENT 的交付目标。

## 二、唯一产出标准：TASK_CURRENT.md 的 14 项浏览器真实验收（红线 #2）

Phase 2 的完成定义只有一条：**TASK_CURRENT.md 列出的 14 项浏览器验收全部通过，
且每项都有"UI → 真实 API → test DB → 刷新后仍保持"的证据。**

- 所有写操作（餐单/食材/菜品）必须走真实 API 落 `assets/test-family_menu.db`。
- 禁止 DOM 假写、localStorage 假数据、mock 业务数据。
- 历史页保持业务只读；只删除 Phase 2 全局"预览锁"。
- **完成某项验收时，必须同步更新 `ACCEPTANCE_TRACKER.md` 对应行（状态 ✅ + 证据路径）。**
  跟踪表是 PM 判定进度的唯一看板，不更新 = 未完成。
- **执行方式以 `EXECUTION_GUIDE.md` 为准**：14 项验收的每一步操作、预期结果、
  刷新验证点、截图点都已写死，照单执行即可，禁止自由发挥或扩展范围。

## 三、提交门禁（红线 #3）

每次提交前必须回答：

1. 本次提交对应 14 项验收中的哪一项？（给编号）
2. 该项的浏览器验收证据在哪？（截图路径 / 测试名 / 报告节）
3. 是否只写了 TASK_CURRENT 允许的文件？是否碰了 rule_engine.py？（碰了 = 违规）

**不满足以上三问的提交 = 无用功，禁止提交。**

## 四、允许范围（红线 #4）

- ✅ 允许：接通餐单页写操作、食材页写操作、菜品页写操作、手机 UI 尺寸修复、
  缺图映射修正、历史页只读读取、新增最小后端端点（必须复用 menu_service/inventory/db）。
- ✅ 允许：为 14 项验收编写验收测试与截图。
- ❌ 禁止：修改正式库/golden、`PUSH_ENABLED=true`、实际推送、破坏性迁移、
  重构无关模块、扫描全项目。

## 五、回报格式（红线 #5）

完成后只回复：

1. 完成了 14 项验收中的哪几项（编号 + 证据路径）
2. 新增/修改文件清单
3. 新增 API 清单（如有）
4. 未完成项及原因

不要输出长报告、完整测试日志、重复项目背景。

---

*门禁签发：PM（2026-08-20）。用户已确认偏差判断，本门禁为强制条款。*
