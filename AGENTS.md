# AGENTS.md — Codex 工作规则

## 默认执行方式

- 先读取 HANDOFF_HOME.md 顶部 CURRENT STATUS。
- 再读取 TASK_CURRENT.md。
- 只处理 TASK_CURRENT.md 里的当前任务。
- 除非任务明确要求，不扫描全项目，不重构无关模块。
- 优先读取与本次任务直接相关的文件。

## 修改范围

- 只修改本次任务明确允许的页面、逻辑、API 或配置。
- 不自动扩大到其他页面或模块。
- 如需新增字段，必须保持向后兼容。
- 不做破坏性数据库迁移。
- 不修改密钥、域名、DNS，除非任务明确要求。

## 部署规则

- 如果 TASK_CURRENT.md 写明“完成后部署上线”，则按现有流程部署。
- 如果涉及正式数据库破坏性操作、密钥变更、域名/DNS变更，必须停止并说明原因。
- 部署后检查线上页面是否生效。

## 输出要求

完成后只说明：

- 修改文件
- 测试结果
- 部署结果
- 未完成问题

不要输出长报告、完整测试日志或重复项目背景。